# ── Core computation ──────────────────────────────────────────────────────────
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0

# ── Network / API ─────────────────────────────────────────────────────────────
flask>=2.0.0
requests>=2.26.0

# Flask pulls these in, but list them explicitly so PyInstaller finds them:
werkzeug>=2.0.0
jinja2>=3.0.0
click>=8.0.0
itsdangerous>=2.0.0

# requests pulls these in:
urllib3>=1.26.0
certifi>=2021.5.30
charset-normalizer>=2.0.0
idna>=3.0

# ── System monitoring ─────────────────────────────────────────────────────────
psutil>=5.8.0
pynvml>=11.0.0      # GPU utilisation display (optional at runtime, needed at build time)

# ── Optional data formats ─────────────────────────────────────────────────────
# h5py>=3.0.0       # .h5 / .hdf5 support — uncomment if needed

# ── GPU acceleration (NOT bundled into exe, install separately if you have CUDA) ──
# cupy-cuda12x      # or cupy-cuda11x depending on your CUDA version

# ── Build / packaging ─────────────────────────────────────────────────────────
pyinstaller>=6.0.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0.0
